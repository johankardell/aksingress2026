apiVersion: apps/v1
kind: Deployment
metadata:
  name: afd-appgw-demo-app
  namespace: demo
  labels:
    app: afd-appgw-demo-app
    demo: afd-appgw
spec:
  replicas: 2
  selector:
    matchLabels:
      app: afd-appgw-demo-app
  template:
    metadata:
      labels:
        app: afd-appgw-demo-app
        demo: afd-appgw
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: app
        image: ${ACR_LOGIN_SERVER}/aks-ingress-demo:${IMAGE_TAG}
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: DEMO_NAME
          value: "Front Door + App Gateway Demo"
        - name: DEMO_TYPE
          value: "Azure Front Door (WAF) + Application Gateway"
        - name: APP_VERSION
          value: "v1-blue"
        - name: APP_BANNER
          value: "AFD + AppGW v1 Blue"
        - name: APP_THEME
          value: "purple"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
